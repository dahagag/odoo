name: Log in to GHCR
description: Log in to the GitHub Container Registry using the workflow's GITHUB_TOKEN.
runs:
  using: composite
  steps:
    - uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ github.token }}
